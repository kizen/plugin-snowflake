{
  "name": "Post to Webhook",
  "action_description": "POSTs a message to a webhook URL. Pair it with the Dad Joke step to send the joke.",
  "plugin_description": "Posts a message to a webhook",
  "action_type": "post_to_webhook",
  "runtime": "python 3.12",
  "secrets": ["webhook_url"],
  "inputs": [
    {
      "name": "message",
      "label": "Message",
      "data_type": "string",
      "required": true,
      "input_source": "variable",
      "script_alias": "message"
    }
  ],
  "outputs": []
}
